<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/green"
    android:effectColor="@color/red">
    <item android:id="@android:id/mask" android:drawable="@color/green" />
    <item android:drawable="@color/lime" />
</ripple>